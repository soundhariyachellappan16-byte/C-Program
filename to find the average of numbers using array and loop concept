#include<stdio.h>
int main()
{
   int i;
   int ages[]={3,4,5};
   float avg , sum =0;
   int length = sizeof(ages)/sizeof(ages[0]);
    for(i=0;i<length;i++){
        sum = sum+ ages[i];
    }
    avg = sum/length;
    printf("the average is %.2f\n", avg);
    return 0;
}
